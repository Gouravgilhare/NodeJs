<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>profile-ejs</title>
      <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>

    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class ="w-full flex justify-end">
            <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block ">logout</a>
        </div>
        
        <h5 class = "mb-3" >Edit Your Post</h5>

        <form action="/Update/<%= post._id %>" method="post">
            <textarea placeholder= "Whats on Your Mind ?"  name  = "content" class="p-3 w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"><%=post.content %></textarea>
            <input type="submit" class="px-3 text-sm   text-black py-2 bg-yellow-500 rounded-md block mt-2 w-40" value=" Update post">
        </form>

    </div>
</body>
</html>